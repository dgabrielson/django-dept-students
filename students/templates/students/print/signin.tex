\documentclass[12pt]{article}

\input{exam_info}

% create an exam_info.tex file containing (e.g.):
% \newcommand{\theCourse}{STAT 1000}
% \newcommand{\theExam}{Midterm 1}
% \newcommand{\theDate}{February 12, 2011}


\usepackage{geometry}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{charter}
\usepackage{fancyhdr}
\usepackage[utf8]{inputenc}


\geometry{margin=0.3in,top=0.75in,bottom=0.4in}
\pagestyle{fancy}
\setlength{\headsep}{15pt}
\setlength{\footskip}{12pt}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}


{% autoescape off %}

\begin{document}
		\lhead{\theCourse: \theExam.}
		\cfoot{\theDate}


%%%%%%
    {% for room, count, start, stop, student_list in object_list %}%
    		\setcounter{page}{1}
		\chead{ {{ room }}, {{ count }} students, {{ start }} -- {{ stop }} }
		\rhead{ Page \thepage\ of \pageref{page:end-{{ forloop.counter }}-mark} }
\begin{longtable}{lllp{0.45\textwidth}}
	\toprule
	\textbf{Name} & \textbf{St.\ Num.} & \textbf{Sec.} & \textbf{Signature \hfill Comments}\\
	\midrule
	\endhead
	& & & \hspace*{\fill} Total \fbox{\phantom{\large MMM}} \\
	\bottomrule
	\endfoot
        {% for name, student_number, section in student_list %}%
        		{{ name }} & {{ student_number }} & {{ section }} &  \\ \midrule
        {% endfor %}%
        	& & & \\ \midrule
        	& & & \\ \midrule
        	& & & \\ \midrule
        	& & & \\ \midrule
        	& & & \\ \midrule
        	& & & \\ \midrule
        	& & &
\end{longtable}

	\vfill

	\begin{flushright}
	Total signatures: \fbox{\phantom{\large MMM}}
	\end{flushright}
    \label{page:end-{{ forloop.counter }}-mark}
        {% if not forloop.last %}%
        \newpage
        {% endif %}%
    {% endfor %}%

\end{document}

{% endautoescape %}
